---
title: "Desktop"
description: "The Tauri app wrapping the same agent."
---

The Tauri app in `desktop/` wraps the same agent with a native window. It shells
out to the `aster` binary on PATH, so `make install` keeps it current.
