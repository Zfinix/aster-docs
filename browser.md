---
title: "Browser"
description: "The chat UI served from localhost."
---

```sh
aster serve
```

Serves the chat UI at `http://localhost:4187` and opens it. Off loopback, a
token is required in the URL, so an exposed port is not an open door.
