{
  "name": "aster-docs",
  "private": true,
  "scripts": {
    "dev": "mint dev",
    "validate": "mint validate",
    "export": "mint export && rm -rf dist .export_tmp && unzip -q export.zip -d .export_tmp && mv .export_tmp/dist dist && rm -rf .export_tmp && node scripts/ai.mjs",
    "deploy": "bun run export && bunx wrangler deploy"
  }
}
